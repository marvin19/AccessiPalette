import React from 'react'

const DisplayResults = () => {
    /**
     * Here I want the different color combinations to be displayed
     * with either checkmark or cross if it is working or not.
     * Search for "Create TailwindCSS Color families(Reddit" to see how the boxes should be like
     * The color boxes should have the hex code written in black or white (check with contrast) to make sure
     * the color is readable.
     * 
     * 
     */
  return (
    <div>DisplayResults</div>
  )
}

export default DisplayResults